general {
    colors = true
    interval = 5
}

order += "volume master"
order += "load"
order += "tztime local"

volume master {
    format = "%volume"
    format_muted = "muted"
    device = "default"
}

tztime local {
    format =  "%Y-%m-%d %a %H:%M:%S"
}